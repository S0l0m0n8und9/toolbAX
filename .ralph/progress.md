# Progress

- Ralph workspace initialized.
- Use this file for durable progress notes between fresh Codex runs.
- Added a testable TestifyConfigurationStore injection seam for DualWriteMapBrowserViewModel, removed ConfigureAwait(false) from the WPF Testify settings load/save path, and added a regression test covering per-map settings load, map switching, save, and reload across a fresh view-model instance. Release solution build passed.
- No further code changes were needed in this slice. Existing T1 work already provides the Testify toolbar entry, in-view per-map settings editor, and persistence/reload coverage. Release build passed.
- Tagged all Testify-focused test classes with Category=Testify so the canonical validator now targets the rollback/configuration coverage already added in the working tree. Release build passed.
- Tightened Testify CE-success reporting so mid-run patch/CE failures are no longer reported as CE success after an initial create verify, and added targeted regression tests for the completed-run result logic. Built successfully on top of the existing rollback helper work already in the tree.
- Extracted FinalizeTestifyFailureAsync so rollback remains limited to records created in the current run, and added regression tests covering fresh-record rollback state clearing plus reused-record rollback skipping. Focused test project build passes.
- No additional code changes were needed in this slice. Verified the existing rollback implementation and regression coverage already in the working tree; canonical Testify validation passed with 26/26 tests.
- Validated the existing auth silent-failure recovery implementation already present in the working tree. Interactive re-auth fallback, plugin-friendly re-auth messaging, and per-environment tenant handling all map to the selected task and pass both targeted auth and full solution tests.
- Installer build now enforces signing inputs and fails fast on the real missing prerequisite instead of producing unsigned artifacts. Existing WiX changes already lock installer GUIDs and bundle the .NET Desktop Runtime.
- Normalized Testify enum coverage gap prepare/detail output to sort missing enum members consistently and updated targeted Testify assertions to match the deterministic format.
- Aligned install/README.md with the already-landed installer changes: documented the locked ProductCode values and clarified that release builds require signing inputs. Release validation now deterministically fails fast on missing signing configuration instead of creating unsigned artifacts.
- Confirmed T4 acceptance criteria are covered by the existing Testify integration/rollback tests, removed one duplicate rollback test, and reran the canonical Testify validator successfully.
- Re-validated the installer pipeline from durable repo state. The source already satisfies the GUID lock, signing invocation, and runtime bundle acceptance criteria; the remaining blocker is environmental because release packaging now correctly fails fast when no signing certificate thumbprint or PFX is supplied.
- Backlog replenishment was needed because `.ralph/tasks.json` had no actionable tasks after T1-T6. Added the next Testify verification slice: T7 FO persisted-value readback, T8 deterministic CE correlation, and T9 CE field reconciliation assertions.
- Added FO persisted-value readback verification after create and after each PATCH, with dedicated readback log phases and mismatch failures. Added targeted integration tests for matching readback plus create/patch mismatch cases.
- Replaced CE count-delta verification with deterministic per-leg CE correlation descriptors, exact correlated-row polling, duplicate/missing/unrelated guards, and patch-time same-row identity enforcement. Added targeted integration coverage for timeout (missing row), duplicate row match, unrelated row, and update row-identity drift.
- Implemented CE correlated-row field reconciliation assertions with per-phase expected/actual checks (create + patch), added value-map output parsing for enum transform validation, surfaced per-field pass/fail in Testify result rows/UI, and added targeted tests for create/patch assertions, scalar normalization, and assertion reporting.
- Backlog replenishment was needed again because T7-T9 completed the prior Testify verification slice and left no actionable tasks. Added T10-T12 to harden assertability: block maps with 0/0 CE assertions, add temporal CE assertion normalization, and surface per-map warning detail for skipped assertions.
- Blocked Testify maps that produce runnable CE legs but zero assertable CE field plans, surfaced skipped-assertion reasons in preflight detail, and prevented completed runs from reporting valid 0/0 CE assertion results. Added targeted regression coverage and the canonical Testify validator passed.
- Enabled direct CE assertions for Edm.Date and Edm.DateTimeOffset, normalized date-only and datetimeoffset comparisons semantically, and added focused temporal pass/mismatch regression coverage.
- Added Testify warning-detail support to preflight and execution result row models, threaded plan warnings through DualWriteMapBrowser Testify row construction, surfaced warning columns in both grids, and added focused model and XAML binding coverage.
- Backlog replenishment was needed again because T10-T12 completed the warning-detail/assertability slice and left no actionable tasks. Added T13-T15 to finish semantic CE assertion comparison for GUID-shaped, boolean-shaped, and null/default mapped outputs that are still treated as raw strings in the current repo state.
- Normalized GUID-shaped CE assertion values so direct Edm.Guid assertions and GUID-looking value-map outputs compare semantically across casing and brace-format differences. Added focused regression coverage and the full Testify validation command passed.
- Normalized boolean-shaped CE value-map and default outputs so 0/1 and false/true compare semantically without weakening existing numeric comparison behavior. Added focused Testify regression coverage for pass and mismatch cases.
- Preserved explicit null CE value-map outputs through transform ingestion, assertion planning, and CE readback formatting. Added focused regression tests for mapped-null pass, explicit null default pass, and null mismatch messaging.
- Re-ran T16 with separate uninstall/install/launch/restart commands, captured per-user runtime evidence under artifacts/t16-per-user-proof-rerun-20260509, confirmed required plugin set on both launches, and verified plugin count parity (5/5). Validation command also passed in explicit build+test steps.
- Executed a deterministic per-machine uninstall/install proof attempt and captured evidence under artifacts/t17-per-machine-proof-20260509. Machine install failed with MSI 1603 / Error 1925 due non-elevated shell, so launch/restart plugin-load proof could not be completed.
- Added runtime migration from legacy flat bundled plugin DLLs to canonical subfolder layout with explicit plugin/path logging for migrated, skipped, and failed cases; added regression tests covering migration and skip diagnostics; required Release build validation passed.
- Added a deterministic legacy flat-layout fixture helper with explicit expected bundled plugin identities and expected count, and wired PluginManager tests to use it for reproducible old-layout staging.
- Test Discover_SameVersionReinstall_Loads_All_Bundled_Plugins_Without_Errors already present in PluginManagerTests.cs (lines 344-382). It pre-stages canonical layout via StageCanonical, runs two successive DiscoverAsync invocations (simulating post-reinstall app restart), and asserts: plugin count == ExpectedBundledPluginCount, plugin IDs match, no Error-level log entries, and plugin root is logged
- Added Discover_Loads_Mixed_Layout_Canonical_And_FlatOnly test covering criterion 3 (mixed layout). HelloPlugin+QueryBuilder staged canonical, TableEntityBrowser+ODataPostBuilder staged flat (auto-migrated by MigrateLegacyFlatBundledPluginsToCanonicalLayout). All 4 load; no duplicate-skip log emitted. All 175 tests pass.
- Added TestifyRunner.RollbackAsync (internal static) that clears LastEntityInstanceUrl unconditionally before DELETE (404=success). Updated FinalizeTestifyFailureAsync to call it. All 4 acceptance criteria met: DELETE issued, 404=success, cache cleared regardless, original exception re-thrown at call site (unchanged). 7 rollback tests pass.
- UserControl TestifyConfigurationPanel fully implemented: all four fields present and editable, Save button triggers SaveCommand, ConfirmationMessage shown inline, modal overlay accessible from toolbar without navigating away. Build passes with 0 warnings.
