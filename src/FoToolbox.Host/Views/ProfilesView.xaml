<UserControl x:Class="FoToolbox.Host.Views.ProfilesView"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:FoToolbox.Core.Models;assembly=FoToolbox.Core"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="1100">
    <UserControl.Resources>
        <Style x:Key="VisibleWhenClientSecret" TargetType="{x:Type FrameworkElement}">
            <Setter Property="Visibility" Value="Collapsed" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Selected.Principal.AuthMode}" Value="{x:Static models:AuthMode.ClientSecret}">
                    <Setter Property="Visibility" Value="Visible" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="VisibleWhenBearerToken" TargetType="{x:Type FrameworkElement}">
            <Setter Property="Visibility" Value="Collapsed" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Selected.Principal.AuthMode}" Value="{x:Static models:AuthMode.BearerToken}">
                    <Setter Property="Visibility" Value="Visible" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="VisibleWhenCertificate" TargetType="{x:Type FrameworkElement}">
            <Setter Property="Visibility" Value="Collapsed" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Selected.Principal.AuthMode}" Value="{x:Static models:AuthMode.Certificate}">
                    <Setter Property="Visibility" Value="Visible" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid Margin="2">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="{DynamicResource Fo.SurfaceAltBrush}"
                BorderBrush="{DynamicResource Fo.BorderBrush}"
                BorderThickness="1"
                Padding="10">
            <DockPanel>
                <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,10,0,0">
                    <Button Content="Add" Command="{Binding AddProfileCommand}" MinWidth="90" Margin="0,0,8,0" />
                    <Button Content="Delete" Command="{Binding DeleteProfileCommand}" MinWidth="90" />
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="Profiles" FontWeight="Bold" FontSize="15" Margin="0,0,0,10" />
                    <ListBox ItemsSource="{Binding Profiles}"
                             SelectedItem="{Binding Selected, Mode=TwoWay}"
                             MinHeight="520"
                             ScrollViewer.HorizontalScrollBarVisibility="Auto"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Environment.Name}" />
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </DockPanel>
        </Border>

        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="16">
            <StackPanel>
                <GroupBox Header="Environment">
                    <Grid Margin="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="170" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Name" VerticalAlignment="Center" />
                        <TextBox Grid.Row="0" Grid.Column="1"
                                 Text="{Binding Selected.Environment.Name, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Base URL" VerticalAlignment="Center" Margin="0,10,0,0" />
                        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,10,0,0"
                                 Text="{Binding Selected.Environment.BaseUrl, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Tenant ID" VerticalAlignment="Center" Margin="0,10,0,0" />
                        <TextBox Grid.Row="2" Grid.Column="1" Margin="0,10,0,0"
                                 Text="{Binding Selected.Environment.TenantId, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Default company" VerticalAlignment="Center" Margin="0,10,0,0" />
                        <TextBox Grid.Row="3" Grid.Column="1" Margin="0,10,0,0"
                                 Text="{Binding Selected.Environment.DefaultCompany, UpdateSourceTrigger=PropertyChanged}" />
                    </Grid>
                </GroupBox>

                <GroupBox Header="Authentication">
                    <Grid Margin="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="170" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Client ID" VerticalAlignment="Center" />
                        <TextBox Grid.Row="0" Grid.Column="1"
                                 Text="{Binding Selected.Principal.ClientId, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Auth mode" VerticalAlignment="Center" Margin="0,10,0,0" />
                        <ComboBox x:Name="AuthModeComboBox" Grid.Row="1" Grid.Column="1" Margin="0,10,0,0" MinWidth="220"
                                  ItemsSource="{Binding AuthModeValues}"
                                  SelectedItem="{Binding Selected.Principal.AuthMode, Mode=TwoWay}" />

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Client secret" VerticalAlignment="Center" Margin="0,10,0,0"
                                   Style="{StaticResource VisibleWhenClientSecret}" />
                        <Grid Grid.Row="2" Grid.Column="1" Margin="0,10,0,0"
                              Style="{StaticResource VisibleWhenClientSecret}">
                            <PasswordBox x:Name="ClientSecretBox" />
                        </Grid>

                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding StoredCredentialStatus}" Foreground="{DynamicResource Fo.SubtleTextBrush}" FontSize="11" Margin="0,5,0,0"
                                   Style="{StaticResource VisibleWhenClientSecret}" />

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Bearer token" VerticalAlignment="Center" Margin="0,10,0,0"
                                   Style="{StaticResource VisibleWhenBearerToken}" />
                        <Grid Grid.Row="4" Grid.Column="1" Margin="0,10,0,0"
                              Style="{StaticResource VisibleWhenBearerToken}">
                            <PasswordBox x:Name="BearerTokenBox" />
                        </Grid>

                        <TextBlock Grid.Row="5" Grid.Column="0" Text="Retrieve token" VerticalAlignment="Center" Margin="0,10,0,0"
                                   Style="{StaticResource VisibleWhenBearerToken}" />
                        <Grid Grid.Row="5" Grid.Column="1" Margin="0,10,0,0"
                              Style="{StaticResource VisibleWhenBearerToken}">
                            <Button Content="Get bearer token"
                                    HorizontalAlignment="Left"
                                    MinWidth="150"
                                    Command="{Binding AcquireBearerTokenCommand}" />
                        </Grid>

                        <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding StoredCredentialStatus}" Foreground="{DynamicResource Fo.SubtleTextBrush}" FontSize="11" Margin="0,5,0,0"
                                   Style="{StaticResource VisibleWhenBearerToken}" />

                        <TextBlock Grid.Row="7" Grid.Column="0" Text="Cert thumbprint" VerticalAlignment="Center" Margin="0,10,0,0"
                                   Style="{StaticResource VisibleWhenCertificate}" />
                        <Grid Grid.Row="7" Grid.Column="1" Margin="0,10,0,0"
                              Style="{StaticResource VisibleWhenCertificate}">
                            <TextBox Text="{Binding Selected.Principal.CertThumbprint, UpdateSourceTrigger=PropertyChanged}" />
                        </Grid>
                    </Grid>
                </GroupBox>

                <WrapPanel Margin="0,2,0,12">
                    <Button Content="Refresh" Command="{Binding RefreshCommand}" MinWidth="98" Margin="0,0,8,0" />
                    <Button Content="Save" Command="{Binding SaveCommand}" MinWidth="98" Margin="0,0,8,0" />
                    <Button Content="Set active" Command="{Binding SetActiveCommand}" MinWidth="110" Margin="0,0,8,0" />
                    <Button Content="Test connection" Command="{Binding TestConnectionCommand}" MinWidth="130" />
                </WrapPanel>

                <TextBlock Text="{Binding Status}" TextWrapping="Wrap" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
