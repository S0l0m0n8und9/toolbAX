<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- App-wide defaults -->
    <Style TargetType="{x:Type Window}">
        <Setter Property="Background" Value="{DynamicResource Fo.WindowBackgroundBrush}" />
        <Setter Property="TextElement.FontFamily" Value="{DynamicResource Fo.FontFamily}" />
        <Setter Property="TextElement.FontSize" Value="{DynamicResource Fo.FontSize.Body}" />
        <Setter Property="TextElement.Foreground" Value="{DynamicResource Fo.TextBrush}" />
    </Style>

    <Style TargetType="{x:Type TextBlock}">
        <Setter Property="TextWrapping" Value="NoWrap" />
    </Style>

    <!-- Buttons (rounded, subtle fill) -->
    <Style TargetType="{x:Type Button}">
        <Setter Property="Background" Value="{DynamicResource Fo.SurfaceAltBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Fo.BorderBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource Fo.BorderThickness.Control}" />
        <Setter Property="Padding" Value="{DynamicResource Fo.Padding.Control}" />
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{DynamicResource Fo.CornerRadius.Control}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Margin="{TemplateBinding Padding}"
                                          RecognizesAccessKey="True" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#FFE9EEF6" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#FFDDE7F7" />
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Fo.AccentPressedBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.55" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TextBox (rounded) -->
    <Style TargetType="{x:Type TextBox}">
        <Setter Property="Background" Value="{DynamicResource Fo.SurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Fo.BorderBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource Fo.BorderThickness.Control}" />
        <Setter Property="Padding" Value="10,6" />
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="{DynamicResource Fo.CornerRadius.Control}"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Margin="{TemplateBinding Padding}"
                                      Focusable="False" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource Fo.AccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.65" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- PasswordBox can't be fully templated the same way without more work; keep it close. -->
    <Style TargetType="{x:Type PasswordBox}">
        <Setter Property="Background" Value="{DynamicResource Fo.SurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Fo.BorderBrush}" />
        <Setter Property="BorderThickness" Value="{DynamicResource Fo.BorderThickness.Control}" />
        <Setter Property="Padding" Value="10,6" />
        <Setter Property="MinHeight" Value="32" />
    </Style>

    <Style TargetType="{x:Type GroupBox}">
        <Setter Property="BorderBrush" Value="{DynamicResource Fo.BorderBrush}" />
        <Setter Property="Background" Value="{DynamicResource Fo.SurfaceBrush}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style TargetType="{x:Type TabControl}">
        <Setter Property="Background" Value="{DynamicResource Fo.SurfaceBrush}" />
    </Style>

    <Style TargetType="{x:Type DataGrid}">
        <Setter Property="Background" Value="{DynamicResource Fo.SurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Fo.BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style TargetType="{x:Type ListBox}">
        <Setter Property="Background" Value="{DynamicResource Fo.SurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Fo.BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style TargetType="{x:Type ListView}">
        <Setter Property="Background" Value="{DynamicResource Fo.SurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Fo.BorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>
</ResourceDictionary>

