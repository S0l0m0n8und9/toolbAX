<Window x:Class="FoToolbox.Host.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="FO Toolbox"
        Width="1400"
        Height="900"
        MinWidth="1280"
        MinHeight="820"
        WindowStartupLocation="CenterScreen"
        Icon="Assets/fo-toolbox.ico">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240" MinWidth="220" />
            <ColumnDefinition Width="6" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Grid.Row="0" Background="{DynamicResource Fo.SurfaceAltBrush}" Padding="10">
            <StackPanel>
                <TextBlock Text="Plugins" FontWeight="Bold" FontSize="15" Margin="0,0,0,10" />
                <ListBox ItemsSource="{Binding Plugins}"
                         SelectedItem="{Binding Selected, Mode=TwoWay}"
                         DisplayMemberPath="Name"
                         MinHeight="500"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         BorderThickness="0" />
            </StackPanel>
        </Border>

        <GridSplitter Grid.Column="1"
                      Grid.Row="0"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      Background="{DynamicResource Fo.BorderBrush}"
                      ShowsPreview="True" />

        <Border Grid.Column="2" Grid.Row="0" Padding="14" Background="{DynamicResource Fo.WindowBackgroundBrush}">
            <ContentControl Content="{Binding ActiveControl}" />
        </Border>

        <Border Grid.Column="0"
                Grid.Row="1"
                Grid.ColumnSpan="3"
                Background="{DynamicResource Fo.SurfaceAltBrush}"
                Padding="8"
                BorderThickness="1"
                BorderBrush="{DynamicResource Fo.BorderBrush}"
                Visibility="{Binding ShowUpdaterUi, Converter={StaticResource BoolToVis}}">
            <DockPanel LastChildFill="True">
                <WrapPanel DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <Button Content="Check updates"
                            Command="{Binding CheckUpdatesCommand}"
                            IsEnabled="{Binding CanCheckUpdates}"
                            Margin="0,0,6,0" />
                    <Button Content="Apply staged update"
                            Command="{Binding ApplyUpdateCommand}"
                            IsEnabled="{Binding HasStagedUpdate}"
                            Margin="0,0,6,0" />
                    <Button Content="Rollback"
                            Command="{Binding RollbackUpdateCommand}"
                            IsEnabled="{Binding HasRollbackUpdate}" />
                </WrapPanel>

                <TextBlock Text="{Binding UpdateStatus}" TextWrapping="Wrap" VerticalAlignment="Center" />
            </DockPanel>
        </Border>
    </Grid>
</Window>
