<Window x:Class="FoToolbox.Host.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:c="clr-namespace:FoToolbox.Host.Controls"
        mc:Ignorable="d"
        Title="toolBax"
        Width="1400"
        Height="900"
        MinWidth="1280"
        MinHeight="820"
        WindowStartupLocation="CenterScreen"
        Icon="Assets/fo-toolbox.ico">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />   <!-- 00 title bar  -->
            <RowDefinition Height="*" />    <!-- 01 content    -->
            <RowDefinition Height="22" />   <!-- 02 status bar -->
        </Grid.RowDefinitions>

        <!-- ── 00 · Title bar ──────────────────────────────────────────── -->
        <Border Grid.Row="0"
                Background="{DynamicResource Fo.Ink0Brush}"
                BorderBrush="{DynamicResource Fo.HairBrush}"
                BorderThickness="0,0,0,1">
            <DockPanel Margin="12,0,8,0" LastChildFill="False">

                <!-- Brand logo + wordmark -->
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left"
                            VerticalAlignment="Center" Margin="0,0,20,0">
                    <Border Width="20" Height="20"
                            Background="{DynamicResource Fo.AccentBrush}"
                            CornerRadius="2" VerticalAlignment="Center">
                        <TextBlock Text="tX"
                                   FontFamily="{DynamicResource Fo.FontFamily.Mono}"
                                   FontWeight="Bold" FontSize="11"
                                   Foreground="{DynamicResource Fo.Ink0Brush}"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center" />
                    </Border>
                    <TextBlock VerticalAlignment="Center" Margin="8,0,0,0"
                               FontFamily="{DynamicResource Fo.FontFamily.Mono}"
                               FontSize="12">
                        <Run Text="tool" Foreground="{DynamicResource Fo.TextBrush}" />
                        <Run Text="B" Foreground="{DynamicResource Fo.AccentBrush}" />
                        <Run Text="ax" Foreground="{DynamicResource Fo.TextBrush}" />
                        <Run Text="  v2.0" Foreground="{DynamicResource Fo.DimTextBrush}" />
                    </TextBlock>
                </StackPanel>

                <!-- Plugin count (far right) -->
                <TextBlock DockPanel.Dock="Right"
                           Text="{Binding PluginCountDisplay}"
                           VerticalAlignment="Center"
                           FontFamily="{DynamicResource Fo.FontFamily.Mono}"
                           FontSize="11"
                           Foreground="{DynamicResource Fo.DimTextBrush}" />

                <!-- Overflow menu button (right, between chip and count) -->
                <Button DockPanel.Dock="Right"
                        Content="&#x22EF;"
                        FontFamily="{DynamicResource Fo.FontFamily.Mono}"
                        FontSize="14"
                        Width="28"
                        Height="22"
                        Margin="6,0,0,0"
                        VerticalAlignment="Center"
                        Style="{DynamicResource Fo.Toolbar.Button}"
                        Click="OnOverflowButtonClick">
                    <Button.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Check updates"
                                      Command="{Binding CheckUpdatesCommand}"
                                      Visibility="{Binding ShowUpdaterUi, Converter={StaticResource BoolToVis}}"
                                      IsEnabled="{Binding CanCheckUpdates}" />
                            <MenuItem Header="Apply update"
                                      Command="{Binding ApplyUpdateCommand}"
                                      Visibility="{Binding ShowUpdaterUi, Converter={StaticResource BoolToVis}}"
                                      IsEnabled="{Binding HasStagedUpdate}" />
                            <MenuItem Header="Rollback to previous"
                                      Command="{Binding RollbackUpdateCommand}"
                                      Visibility="{Binding ShowUpdaterUi, Converter={StaticResource BoolToVis}}"
                                      IsEnabled="{Binding HasRollbackUpdate}" />
                            <Separator Visibility="{Binding ShowUpdaterUi, Converter={StaticResource BoolToVis}}" />
                            <MenuItem Header="About toolBax" Command="{Binding ShowAboutCommand}" />
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>

                <!-- Profile chip (right, leftmost of chip/button/count trio) -->
                <c:ProfileChip DockPanel.Dock="Right"
                               VerticalAlignment="Center"
                               Margin="0,0,12,0"
                               Profiles="{Binding ProfilesViewModelHost.Profiles}"
                               ActiveProfileName="{Binding Shell.ActiveProfileName, TargetNullValue='No profile'}"
                               ConnectionStatus="{Binding Shell.ConnectionStatus}"
                               SetActiveProfileCommand="{Binding ProfilesViewModelHost.SetActiveProfileByItemCommand}"
                               OpenProfilesCommand="{Binding NavigateToProfilesCommand}" />
            </DockPanel>
        </Border>

        <!-- ── 01 · Content area ───────────────────────────────────────── -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="48" />  <!-- left rail -->
                <ColumnDefinition Width="*" />   <!-- main area -->
            </Grid.ColumnDefinitions>

            <!-- Left icon rail -->
            <Border Grid.Column="0"
                    Background="{DynamicResource Fo.Ink0Brush}"
                    BorderBrush="{DynamicResource Fo.HairBrush}"
                    BorderThickness="0,0,1,0">
                <ListBox Style="{DynamicResource Fo.LeftRail.ListBox}"
                         AutomationProperties.AutomationId="FoNavRail"
                         ItemsSource="{Binding Plugins}"
                         SelectedItem="{Binding Selected, Mode=TwoWay}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Viewbox Width="16" Height="16" Stretch="Uniform"
                                     ToolTip="{Binding Name}">
                                <Canvas Width="24" Height="24">
                                    <Path Data="{Binding IconGeometry}"
                                          Fill="Transparent"
                                          StrokeThickness="1.5"
                                          StrokeLineJoin="Round"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round">
                                        <Path.Style>
                                            <Style TargetType="Path">
                                                <Setter Property="Stroke"
                                                        Value="{DynamicResource Fo.SubtleTextBrush}" />
                                                <Style.Triggers>
                                                    <DataTrigger Value="True"
                                                                 Binding="{Binding IsSelected,
                                                                     RelativeSource={RelativeSource
                                                                         AncestorType=ListBoxItem}}">
                                                        <Setter Property="Stroke"
                                                                Value="{DynamicResource Fo.AccentBrush}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Path.Style>
                                    </Path>
                                </Canvas>
                            </Viewbox>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <!-- Main area: tab bar + plugin content -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="32" />  <!-- tab bar -->
                    <RowDefinition Height="*" />   <!-- content -->
                </Grid.RowDefinitions>

                <!-- Tab bar -->
                <Border Grid.Row="0"
                        Background="{DynamicResource Fo.Ink0Brush}"
                        BorderBrush="{DynamicResource Fo.HairBrush}"
                        BorderThickness="0,0,0,1">
                    <ListBox Style="{DynamicResource Fo.TabBar.ListBox}"
                             AutomationProperties.AutomationId="FoTabBar"
                             ItemsSource="{Binding Plugins}"
                             SelectedItem="{Binding Selected, Mode=TwoWay}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Viewbox Width="12" Height="12" Stretch="Uniform"
                                             Margin="0,0,6,0">
                                        <Canvas Width="24" Height="24">
                                            <Path Data="{Binding IconGeometry}"
                                                  Fill="Transparent"
                                                  StrokeThickness="1.5"
                                                  StrokeLineJoin="Round"
                                                  StrokeStartLineCap="Round"
                                                  StrokeEndLineCap="Round">
                                                <Path.Style>
                                                    <Style TargetType="Path">
                                                        <Setter Property="Stroke"
                                                                Value="{DynamicResource Fo.SubtleTextBrush}" />
                                                        <Style.Triggers>
                                                            <DataTrigger Value="True"
                                                                         Binding="{Binding IsSelected,
                                                                             RelativeSource={RelativeSource
                                                                                 AncestorType=ListBoxItem}}">
                                                                <Setter Property="Stroke"
                                                                        Value="{DynamicResource Fo.AccentBrush}" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Path.Style>
                                            </Path>
                                        </Canvas>
                                    </Viewbox>
                                    <TextBlock Text="{Binding Name}"
                                               FontSize="12"
                                               VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground"
                                                        Value="{DynamicResource Fo.SubtleTextBrush}" />
                                                <Style.Triggers>
                                                    <DataTrigger Value="True"
                                                                 Binding="{Binding IsSelected,
                                                                     RelativeSource={RelativeSource
                                                                         AncestorType=ListBoxItem}}">
                                                        <Setter Property="Foreground"
                                                                Value="{DynamicResource Fo.TextBrush}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Border>

                <!-- Plugin content -->
                <ContentControl Grid.Row="1"
                                AutomationProperties.AutomationId="FoPluginContent"
                                Content="{Binding ActiveControl}"
                                Background="{DynamicResource Fo.Ink1Brush}" />
            </Grid>
        </Grid>

        <!-- ── 02 · Status bar ─────────────────────────────────────────── -->
        <Border Grid.Row="2"
                Background="{DynamicResource Fo.Ink0Brush}"
                BorderBrush="{DynamicResource Fo.HairBrush}"
                BorderThickness="0,1,0,0">
            <DockPanel>

                <!-- Active plugin -->
                <Border DockPanel.Dock="Left"
                        BorderBrush="{DynamicResource Fo.HairBrush}"
                        BorderThickness="0,0,1,0"
                        Padding="10,0">
                    <TextBlock Text="{Binding Selected.Name, FallbackValue=''}"
                               VerticalAlignment="Center"
                               FontFamily="{DynamicResource Fo.FontFamily.Mono}"
                               FontSize="10.5"
                               Foreground="{DynamicResource Fo.SubtleTextBrush}" />
                </Border>

                <!-- Active profile echo with status pip -->
                <Border DockPanel.Dock="Left"
                        BorderBrush="{DynamicResource Fo.HairBrush}"
                        BorderThickness="0,0,1,0"
                        Padding="10,0">
                    <c:StatusPip State="{Binding ShellPipState}"
                                 Label="{Binding Shell.ActiveProfileName, TargetNullValue='No profile'}" />
                </Border>

                <!-- Busy indicator (only when busy) -->
                <Border DockPanel.Dock="Left"
                        BorderBrush="{DynamicResource Fo.HairBrush}"
                        BorderThickness="0,0,1,0"
                        Padding="10,0"
                        Visibility="{Binding Shell.IsBusy, Converter={StaticResource BoolToVis}}">
                    <c:StatusPip State="Busy" Label="working..." />
                </Border>

                <!-- Update channel (right, only when updater configured) -->
                <Border DockPanel.Dock="Right"
                        BorderBrush="{DynamicResource Fo.HairBrush}"
                        BorderThickness="1,0,0,0"
                        Padding="10,0"
                        Visibility="{Binding ShowUpdaterUi, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="{Binding UpdateChannel}"
                               VerticalAlignment="Center"
                               FontFamily="{DynamicResource Fo.FontFamily.Mono}"
                               FontSize="10.5"
                               Foreground="{DynamicResource Fo.DimTextBrush}" />
                </Border>

                <!-- Update staged star -->
                <Border DockPanel.Dock="Right"
                        BorderBrush="{DynamicResource Fo.HairBrush}"
                        BorderThickness="1,0,0,0"
                        Padding="10,0"
                        Visibility="{Binding HasStagedUpdate, Converter={StaticResource BoolToVis}}">
                    <TextBlock VerticalAlignment="Center"
                               FontFamily="{DynamicResource Fo.FontFamily.Mono}"
                               FontSize="10.5"
                               Foreground="{DynamicResource Fo.AccentBrush}">
                        <Run Text="&#x2605; update ready" />
                    </TextBlock>
                </Border>

                <!-- Last connection ping -->
                <Border DockPanel.Dock="Right"
                        BorderBrush="{DynamicResource Fo.HairBrush}"
                        BorderThickness="1,0,0,0"
                        Padding="10,0"
                        Visibility="{Binding Shell.HasLastPing, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="{Binding Shell.LastPingDisplay}"
                               VerticalAlignment="Center"
                               FontFamily="{DynamicResource Fo.FontFamily.Mono}"
                               FontSize="10.5"
                               Foreground="{DynamicResource Fo.SubtleTextBrush}" />
                </Border>

                <Grid />
            </DockPanel>
        </Border>
    </Grid>
</Window>
